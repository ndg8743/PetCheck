# PetCheck Backend Environment Configuration
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control

# ================================
# Server Configuration
# ================================
PORT=3001
NODE_ENV=development

# ================================
# Redis Configuration
# ================================
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_PASSWORD=your-redis-password-if-required
REDIS_DB=0

# ================================
# JWT Authentication
# ================================
# IMPORTANT: Generate a strong secret key for production
# Example: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# ================================
# Google OAuth 2.0
# ================================
# Get credentials from: https://console.cloud.google.com/
# 1. Create a new project or select existing
# 2. Enable Google+ API
# 3. Create OAuth 2.0 credentials
# 4. Add authorized redirect URIs:
#    - http://localhost:5173/auth/google/callback (development)
#    - https://yourdomain.com/auth/google/callback (production)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ================================
# Google Places API
# ================================
# Get API key from: https://console.cloud.google.com/
# Enable Places API in your Google Cloud project
GOOGLE_PLACES_API_KEY=your-google-places-api-key

# ================================
# OpenFDA API
# ================================
# Optional but recommended for higher rate limits
# Request API key at: https://open.fda.gov/apis/authentication/
# Without key: 40 requests/minute, 1,000/day
# With key: 240 requests/minute, 120,000/day
OPENFDA_API_KEY=your-openfda-api-key

# ================================
# Application URLs
# ================================
# Frontend URL for CORS and redirects
FRONTEND_URL=http://localhost:5173

# Backend API base URL
API_BASE_URL=http://localhost:3001

# ================================
# CORS Configuration
# ================================
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:5173,http://localhost:5174

# ================================
# Logging
# ================================
# Log level: debug, info, warn, error
LOG_LEVEL=info

# Log format: combined, common, dev, short, tiny, json
LOG_FORMAT=combined

# ================================
# Rate Limiting
# ================================
# API rate limit (requests per minute per IP)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_MAX_SEARCH_REQUESTS=30

# ================================
# Cache TTL (Time To Live in seconds)
# ================================
CACHE_ADVERSE_EVENTS=3600
CACHE_RECALLS=1800
CACHE_DRUGS=86400
CACHE_INTERACTIONS=86400
CACHE_GREEN_BOOK=604800
CACHE_USER_SESSION=604800

# ================================
# Production Environment Variables
# ================================
# Uncomment and configure these for production deployment

# NODE_ENV=production
# PORT=3001
# REDIS_HOST=your-production-redis-host
# REDIS_PASSWORD=your-production-redis-password
# JWT_SECRET=your-production-jwt-secret
# FRONTEND_URL=https://yourdomain.com
# API_BASE_URL=https://api.yourdomain.com
# CORS_ORIGINS=https://yourdomain.com
# LOG_LEVEL=warn

# ================================
# Optional: Database Configuration
# ================================
# If you add PostgreSQL/MongoDB in the future
# DATABASE_URL=postgresql://user:password@localhost:5432/petcheck
# MONGODB_URI=mongodb://localhost:27017/petcheck

# ================================
# Optional: Email Service
# ================================
# If you add email notifications
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASSWORD=your-email-password
# EMAIL_FROM=noreply@petcheck.com

# ================================
# Optional: Monitoring & Analytics
# ================================
# SENTRY_DSN=your-sentry-dsn
# GOOGLE_ANALYTICS_ID=your-ga-id
# NEW_RELIC_LICENSE_KEY=your-new-relic-key

# ================================
# Optional: AWS Services
# ================================
# If you add S3 storage for pet photos, etc.
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_REGION=us-east-1
# S3_BUCKET_NAME=petcheck-uploads

# ================================
# Notes
# ================================
# 1. Never commit the .env file to version control
# 2. Use strong, unique values for JWT_SECRET in production
# 3. Rotate secrets regularly for security
# 4. Use environment-specific .env files (.env.development, .env.production)
# 5. Consider using a secrets manager for production (AWS Secrets Manager, HashiCorp Vault, etc.)
